# ═══════════════════════════════════════════════════════════════════════════
# NYC Micro-Climate Zoning AI — Data Acquisition Dependencies
# ═══════════════════════════════════════════════════════════════════════════
# Install:  pip install -r requirements.txt
# Optional: pip install -r requirements.txt -r requirements-remote.txt
# ═══════════════════════════════════════════════════════════════════════════

# ── Core: Vector GIS & Data Processing ───────────────────────────────────
# Handles GeoJSON, Shapefile, CSV ingestion; CRS transforms; spatial ops
geopandas>=1.0.0            # Spatial DataFrames (Building Footprints, MapPLUTO, Tree Census)
fiona>=1.9.0                # OGR-based vector I/O backend for geopandas
shapely>=2.0.0              # Geometry engine (spatial predicates, buffering)
pyproj>=3.6.0               # CRS definitions and coordinate transforms (EPSG:4326↔2263)
pandas>=2.0.0               # Tabular data processing (MapPLUTO CSV, Tree Census CSV)
numpy>=1.24.0               # Numerical arrays (raster math, NDVI, temperature grids)

# ── Core: HTTP & Download Infrastructure ─────────────────────────────────
requests>=2.31.0            # HTTP downloads (NYC Open Data Socrata API)
tqdm>=4.65.0                # Progress bars for long downloads and batch processing

# ── Raster Processing ───────────────────────────────────────────────────
# Required for Landsat 8, Sentinel-2 GeoTIFF validation & future NDVI/LST
rasterio>=1.3.0             # GeoTIFF read/write/validate (backed by GDAL)
rasterstats>=0.19.0         # Zonal statistics (raster values per building polygon)

# ── Weather / Climate Data ───────────────────────────────────────────────
# Required for ERA5 NetCDF ingestion and validation
netCDF4>=1.6.0              # NetCDF-4 file I/O (ERA5 hourly reanalysis)
h5py>=3.9.0                 # HDF5 backend (NetCDF-4 uses HDF5 internally)
xarray>=2023.6.0            # Labelled N-D arrays; ergonomic NetCDF access
scipy>=1.11.0               # Spatial/temporal interpolation of climate grids

# ── Reporting & Validation ───────────────────────────────────────────────
tabulate>=0.9.0             # Pretty-printed Markdown tables in reports
jsonschema>=4.17.0          # Schema validation for metadata JSON files
