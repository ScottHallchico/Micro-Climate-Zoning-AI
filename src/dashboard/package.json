{
  "name": "dashboard",
  "version": "1.0.0",
  "description": "Mumbai 3D District Map dashboard",
  "type": "module",
  "scripts": {
    "test": "node --experimental-vm-modules node_modules/.bin/jest",
    "test:run": "node --experimental-vm-modules node_modules/.bin/jest --runInBand"
  },
  "devDependencies": {
    "jest": "^29.7.0",
    "fast-check": "^3.15.0"
  },
  "jest": {
    "testEnvironment": "node",
    "transform": {},
    "testMatch": [
      "**/tests/**/*.test.js"
    ]
  }
}
